// Code generated by ent, DO NOT EDIT.

package neopg

import (
	"fmt"
	"haruki-suite/utils/database/neopg/hourlyrequests"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// HourlyRequests is the model entity for the HourlyRequests schema.
type HourlyRequests struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Primary key datetime (hour key)
	HourKey time.Time `json:"hour_key,omitempty"`
	// Request count for this hour
	Count        int `json:"count,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HourlyRequests) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case hourlyrequests.FieldID, hourlyrequests.FieldCount:
			values[i] = new(sql.NullInt64)
		case hourlyrequests.FieldHourKey:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HourlyRequests fields.
func (_m *HourlyRequests) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case hourlyrequests.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case hourlyrequests.FieldHourKey:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field hour_key", values[i])
			} else if value.Valid {
				_m.HourKey = value.Time
			}
		case hourlyrequests.FieldCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field count", values[i])
			} else if value.Valid {
				_m.Count = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the HourlyRequests.
// This includes values selected through modifiers, order, etc.
func (_m *HourlyRequests) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this HourlyRequests.
// Note that you need to call HourlyRequests.Unwrap() before calling this method if this HourlyRequests
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *HourlyRequests) Update() *HourlyRequestsUpdateOne {
	return NewHourlyRequestsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the HourlyRequests entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *HourlyRequests) Unwrap() *HourlyRequests {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("neopg: HourlyRequests is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *HourlyRequests) String() string {
	var builder strings.Builder
	builder.WriteString("HourlyRequests(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("hour_key=")
	builder.WriteString(_m.HourKey.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("count=")
	builder.WriteString(fmt.Sprintf("%v", _m.Count))
	builder.WriteByte(')')
	return builder.String()
}

// HourlyRequestsSlice is a parsable slice of HourlyRequests.
type HourlyRequestsSlice []*HourlyRequests
