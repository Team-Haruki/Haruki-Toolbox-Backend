// Code generated by ent, DO NOT EDIT.

package neopg

import (
	"fmt"
	"haruki-suite/utils/database/neopg/dailyrequests"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// DailyRequests is the model entity for the DailyRequests schema.
type DailyRequests struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Primary key date
	DateKey time.Time `json:"date_key,omitempty"`
	// Request count for this date
	Count        int `json:"count,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DailyRequests) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case dailyrequests.FieldID, dailyrequests.FieldCount:
			values[i] = new(sql.NullInt64)
		case dailyrequests.FieldDateKey:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DailyRequests fields.
func (_m *DailyRequests) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case dailyrequests.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case dailyrequests.FieldDateKey:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field date_key", values[i])
			} else if value.Valid {
				_m.DateKey = value.Time
			}
		case dailyrequests.FieldCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field count", values[i])
			} else if value.Valid {
				_m.Count = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the DailyRequests.
// This includes values selected through modifiers, order, etc.
func (_m *DailyRequests) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this DailyRequests.
// Note that you need to call DailyRequests.Unwrap() before calling this method if this DailyRequests
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *DailyRequests) Update() *DailyRequestsUpdateOne {
	return NewDailyRequestsClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the DailyRequests entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *DailyRequests) Unwrap() *DailyRequests {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("neopg: DailyRequests is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *DailyRequests) String() string {
	var builder strings.Builder
	builder.WriteString("DailyRequests(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("date_key=")
	builder.WriteString(_m.DateKey.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("count=")
	builder.WriteString(fmt.Sprintf("%v", _m.Count))
	builder.WriteByte(')')
	return builder.String()
}

// DailyRequestsSlice is a parsable slice of DailyRequests.
type DailyRequestsSlice []*DailyRequests
