// Code generated by ent, DO NOT EDIT.

package hook

import (
	"context"
	"fmt"
	"haruki-suite/utils/database/postgresql"
)

// The AuthorizeSocialPlatformInfoFunc type is an adapter to allow the use of ordinary
// function as AuthorizeSocialPlatformInfo mutator.
type AuthorizeSocialPlatformInfoFunc func(context.Context, *postgresql.AuthorizeSocialPlatformInfoMutation) (postgresql.Value, error)

// Mutate calls f(ctx, m).
func (f AuthorizeSocialPlatformInfoFunc) Mutate(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
	if mv, ok := m.(*postgresql.AuthorizeSocialPlatformInfoMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *postgresql.AuthorizeSocialPlatformInfoMutation", m)
}

// The EmailInfoFunc type is an adapter to allow the use of ordinary
// function as EmailInfo mutator.
type EmailInfoFunc func(context.Context, *postgresql.EmailInfoMutation) (postgresql.Value, error)

// Mutate calls f(ctx, m).
func (f EmailInfoFunc) Mutate(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
	if mv, ok := m.(*postgresql.EmailInfoMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *postgresql.EmailInfoMutation", m)
}

// The GameAccountBindingFunc type is an adapter to allow the use of ordinary
// function as GameAccountBinding mutator.
type GameAccountBindingFunc func(context.Context, *postgresql.GameAccountBindingMutation) (postgresql.Value, error)

// Mutate calls f(ctx, m).
func (f GameAccountBindingFunc) Mutate(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
	if mv, ok := m.(*postgresql.GameAccountBindingMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *postgresql.GameAccountBindingMutation", m)
}

// The GroupFunc type is an adapter to allow the use of ordinary
// function as Group mutator.
type GroupFunc func(context.Context, *postgresql.GroupMutation) (postgresql.Value, error)

// Mutate calls f(ctx, m).
func (f GroupFunc) Mutate(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
	if mv, ok := m.(*postgresql.GroupMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *postgresql.GroupMutation", m)
}

// The GroupListFunc type is an adapter to allow the use of ordinary
// function as GroupList mutator.
type GroupListFunc func(context.Context, *postgresql.GroupListMutation) (postgresql.Value, error)

// Mutate calls f(ctx, m).
func (f GroupListFunc) Mutate(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
	if mv, ok := m.(*postgresql.GroupListMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *postgresql.GroupListMutation", m)
}

// The IOSScriptCodeFunc type is an adapter to allow the use of ordinary
// function as IOSScriptCode mutator.
type IOSScriptCodeFunc func(context.Context, *postgresql.IOSScriptCodeMutation) (postgresql.Value, error)

// Mutate calls f(ctx, m).
func (f IOSScriptCodeFunc) Mutate(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
	if mv, ok := m.(*postgresql.IOSScriptCodeMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *postgresql.IOSScriptCodeMutation", m)
}

// The SocialPlatformInfoFunc type is an adapter to allow the use of ordinary
// function as SocialPlatformInfo mutator.
type SocialPlatformInfoFunc func(context.Context, *postgresql.SocialPlatformInfoMutation) (postgresql.Value, error)

// Mutate calls f(ctx, m).
func (f SocialPlatformInfoFunc) Mutate(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
	if mv, ok := m.(*postgresql.SocialPlatformInfoMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *postgresql.SocialPlatformInfoMutation", m)
}

// The UploadLogFunc type is an adapter to allow the use of ordinary
// function as UploadLog mutator.
type UploadLogFunc func(context.Context, *postgresql.UploadLogMutation) (postgresql.Value, error)

// Mutate calls f(ctx, m).
func (f UploadLogFunc) Mutate(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
	if mv, ok := m.(*postgresql.UploadLogMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *postgresql.UploadLogMutation", m)
}

// The UserFunc type is an adapter to allow the use of ordinary
// function as User mutator.
type UserFunc func(context.Context, *postgresql.UserMutation) (postgresql.Value, error)

// Mutate calls f(ctx, m).
func (f UserFunc) Mutate(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
	if mv, ok := m.(*postgresql.UserMutation); ok {
		return f(ctx, mv)
	}
	return nil, fmt.Errorf("unexpected mutation type %T. expect *postgresql.UserMutation", m)
}

// Condition is a hook condition function.
type Condition func(context.Context, postgresql.Mutation) bool

// And groups conditions with the AND operator.
func And(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m postgresql.Mutation) bool {
		if !first(ctx, m) || !second(ctx, m) {
			return false
		}
		for _, cond := range rest {
			if !cond(ctx, m) {
				return false
			}
		}
		return true
	}
}

// Or groups conditions with the OR operator.
func Or(first, second Condition, rest ...Condition) Condition {
	return func(ctx context.Context, m postgresql.Mutation) bool {
		if first(ctx, m) || second(ctx, m) {
			return true
		}
		for _, cond := range rest {
			if cond(ctx, m) {
				return true
			}
		}
		return false
	}
}

// Not negates a given condition.
func Not(cond Condition) Condition {
	return func(ctx context.Context, m postgresql.Mutation) bool {
		return !cond(ctx, m)
	}
}

// HasOp is a condition testing mutation operation.
func HasOp(op postgresql.Op) Condition {
	return func(_ context.Context, m postgresql.Mutation) bool {
		return m.Op().Is(op)
	}
}

// HasAddedFields is a condition validating `.AddedField` on fields.
func HasAddedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m postgresql.Mutation) bool {
		if _, exists := m.AddedField(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.AddedField(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasClearedFields is a condition validating `.FieldCleared` on fields.
func HasClearedFields(field string, fields ...string) Condition {
	return func(_ context.Context, m postgresql.Mutation) bool {
		if exists := m.FieldCleared(field); !exists {
			return false
		}
		for _, field := range fields {
			if exists := m.FieldCleared(field); !exists {
				return false
			}
		}
		return true
	}
}

// HasFields is a condition validating `.Field` on fields.
func HasFields(field string, fields ...string) Condition {
	return func(_ context.Context, m postgresql.Mutation) bool {
		if _, exists := m.Field(field); !exists {
			return false
		}
		for _, field := range fields {
			if _, exists := m.Field(field); !exists {
				return false
			}
		}
		return true
	}
}

// If executes the given hook under condition.
//
//	hook.If(ComputeAverage, And(HasFields(...), HasAddedFields(...)))
func If(hk postgresql.Hook, cond Condition) postgresql.Hook {
	return func(next postgresql.Mutator) postgresql.Mutator {
		return postgresql.MutateFunc(func(ctx context.Context, m postgresql.Mutation) (postgresql.Value, error) {
			if cond(ctx, m) {
				return hk(next).Mutate(ctx, m)
			}
			return next.Mutate(ctx, m)
		})
	}
}

// On executes the given hook only for the given operation.
//
//	hook.On(Log, postgresql.Delete|postgresql.Create)
func On(hk postgresql.Hook, op postgresql.Op) postgresql.Hook {
	return If(hk, HasOp(op))
}

// Unless skips the given hook only for the given operation.
//
//	hook.Unless(Log, postgresql.Update|postgresql.UpdateOne)
func Unless(hk postgresql.Hook, op postgresql.Op) postgresql.Hook {
	return If(hk, Not(HasOp(op)))
}

// FixedError is a hook returning a fixed error.
func FixedError(err error) postgresql.Hook {
	return func(postgresql.Mutator) postgresql.Mutator {
		return postgresql.MutateFunc(func(context.Context, postgresql.Mutation) (postgresql.Value, error) {
			return nil, err
		})
	}
}

// Reject returns a hook that rejects all operations that match op.
//
//	func (T) Hooks() []postgresql.Hook {
//		return []postgresql.Hook{
//			Reject(postgresql.Delete|postgresql.Update),
//		}
//	}
func Reject(op postgresql.Op) postgresql.Hook {
	hk := FixedError(fmt.Errorf("%s operation is not allowed", op))
	return On(hk, op)
}

// Chain acts as a list of hooks and is effectively immutable.
// Once created, it will always hold the same set of hooks in the same order.
type Chain struct {
	hooks []postgresql.Hook
}

// NewChain creates a new chain of hooks.
func NewChain(hooks ...postgresql.Hook) Chain {
	return Chain{append([]postgresql.Hook(nil), hooks...)}
}

// Hook chains the list of hooks and returns the final hook.
func (c Chain) Hook() postgresql.Hook {
	return func(mutator postgresql.Mutator) postgresql.Mutator {
		for i := len(c.hooks) - 1; i >= 0; i-- {
			mutator = c.hooks[i](mutator)
		}
		return mutator
	}
}

// Append extends a chain, adding the specified hook
// as the last ones in the mutation flow.
func (c Chain) Append(hooks ...postgresql.Hook) Chain {
	newHooks := make([]postgresql.Hook, 0, len(c.hooks)+len(hooks))
	newHooks = append(newHooks, c.hooks...)
	newHooks = append(newHooks, hooks...)
	return Chain{newHooks}
}

// Extend extends a chain, adding the specified chain
// as the last ones in the mutation flow.
func (c Chain) Extend(chain Chain) Chain {
	return c.Append(chain.hooks...)
}
